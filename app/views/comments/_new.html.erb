<%= form_with(model: [ @topic, Comment.new ], remote: true) do |form| %>
    Your Comment:<br>
    <%= form.hidden_field :content, id: :topic_comments %>
    <trix-editor input="topic_comments"></trix-editor>
    <%= form.submit %>
<% end %> 